const fs = require('fs'); const path = require('path'); function walk(dir) { let found = []; fs.readdirSync(dir).forEach(f => { let p = path.join(dir, f); if (fs.statSync(p).isDirectory() && !p.includes('node_modules') && !p.includes('.git') && !p.includes('dist')) { found = found.concat(walk(p)); } else if (p.endsWith('.tsx') || p.endsWith('.ts')) { let str = fs.readFileSync(p, 'utf8'); if (str.includes('Ã')) { found.push(p); } } }); return found; } const files = walk('y:/2026/ERP ESC LOG/Orbe/src'); console.log(files);
