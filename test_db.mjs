import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function run() { const { data: emp, error } = await supabase.from('empresas').select('*').limit(1); const { data: trans } = await supabase.from('transportadoras').select('*').limit(1); const { data: colabs } = await supabase.from('colaboradores').select('*').limit(1); const { data: rules } = await supabase.from('regras_operacionais').select('*').limit(1); console.log('Emp:', emp, '\nTrans:', trans, '\nColabs:', colabs, '\nRules:', rules); } run();
